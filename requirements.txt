streamlit>=1.37
playwright>=1.44
# Note: v2.0 of playwright-stealth changed the API. The crawler tolerates both,
# but pinning to v1.x avoids the most common silent-failure mode.
playwright-stealth>=1.0.6,<2.0
pandas>=2.2
openpyxl>=3.1
PyYAML>=6.0
beautifulsoup4>=4.12
lxml>=5.2
python-dateutil>=2.9
# Optional: enable Redis-backed cache/queue. Comment out if unused.
redis>=5.0
